function strings(str1, str2) {
  //Gets length of string with .length property and prints it and formats the output with template literal string.
  console.log(`${str1.length} ${str2.length}`);
  //Prints first and second string, seperated with template literal.
  console.log(`${str1} ${str2}`);
}
